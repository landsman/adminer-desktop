
/.cache
/.idea/
/out/
/.playwright-mcp/
# Throwaway git worktrees an agent checks out to work in isolation — a copy of the repo
# each, and gone once the work lands. Only the worktrees: .claude/skills/ is committed,
# because a skill is documentation the next person reads.
/.claude/worktrees/
# Installed dependencies — composer.json/lock and package.json/lock are the source of
# truth; the trees themselves are built by `mise run install`, not committed.
/node_modules
/app/vendor/
# e2e artifacts: screenshots the browser check writes.
/tests/e2e/screenshots/
/app/adminer.php
/app/editor.php
/app/src/Settings/Plugins/available
# The gallery designs are fetched, but adminer-desktop/ is our own theme — keep it. The
# /* form ignores each fetched design individually so the one exception can be negated;
# a blanket designs/ would stop git descending and the negation could not take effect.
/app/src/Settings/Theme/designs/*
!/app/src/Settings/Theme/designs/adminer-desktop/
/bin
/build/
# Native-shell translations: generated from app/src/I18n/Locale/*.php by `make i18n` and rebuilt
# before every build. The per-language files are the committed source.
/lproj/*.lproj/Localizable.strings
/launcher/i18n_gen.h
# Checked-in checksums are what make it reproducible — the bytes don't belong in git.
# Everything below is downloaded by `make fetch` at a pinned version.
# The enabled-plugin set is user state, not source.
.DS_Store
